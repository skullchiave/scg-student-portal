# SCG 学生ポータル — アプリの正典

学生が自分のスマホでログインする**学生への窓口**。scg-mobile-portal（先生用GASランチャー）とは別物。
中身＝小テスト／アンケート／告知／進路情報／アルバイト情報。

> 🔴 **運用・役割分担・きあの手番・権利と同意は、ここではなくドライブ側に置く。**
> `0.2 claude-work\【学生ポータル】 Student Portal\CLAUDE.md`（運用の正典）
> ここ（リポ）はアプリの中身だけ＝構成・実装ルール・テスト・スキーマ・技術的な未決。**両方に同じことを書かない。**

## 現在地（2026-09-06）

2026-09-04 のMTGで全面採用が決定し、同時にヨリソルの契約打ち切りも決まった。
**フォールバックが消えた**ので、判断軸は **「2027年4月に間に合うか」** と **「佐々木が不在でも回るか」** の2つ。
契約終了は **2027-02-28**（データはそれ以降取り出せない）。実運用は **2027-04**。
規模は午前・午後で150名ずつ（来年度は最大250名）＝**実測済みの150名がそのまま本番規模**。
※ 背景・締切表・関係者はドライブ側の CLAUDE.md。

## 構成

- 画面は **3つ**（2026-09-12 きあ提案で分けた）。→ GitHub Pages（Actions で `src/` だけをデプロイ）

  | ファイル | 誰が | 何が置いてあるか |
  |---|---|---|
  | `src/index.html` | 学生 `s001〜` | 小テスト・アンケート・つぎの しけん |
  | `src/teacher.html` | 先生 `t001〜` | **授業中に使うものだけ**＝📅今日／テストをえらんで はじめる／ライブ集計／結果のCSV／学生用QR |
  | `src/master.html` | マスター `m001〜` | **机の仕事**＝問題の登録・公開・削除／アンケートの作成・回答一覧／お知らせ／点検 |

  - ★**ログインIDの頭文字で役割が分かる**（s/t/m）。人に説明するときに効く
  - ⚠ `master.html` は **2026-09-12 に `teacher.html` から改名**したもの。
    いまの `teacher.html` は**新しく小さく書き起こしたもの**で、中身は別物。
    ★**master から機能を削って作らなかった**のは、削る作り方には消し忘れが必ず残るため
  - 🔴 **いまは画面だけが分かれていて、権限（RLS）は先生とマスターで同じ。**
    `app_hidden.is_teacher()` は名前に反して「**職員（teacher または master）**」を意味する
    （18のポリシーと8つの関数から呼ばれていて、今すぐ書き換えると1つ落としただけで静かに権限が消える）。
    先生とマスターで分けたいところは `app_hidden.is_master()` を使う。
    **権限を分けるのは「先生ごとのアカウントを作る」のと同時**（E領域・4月まで）
  - 画面が「自分は何者か」を聞くのは `public.my_role()`。★**これは案内であって権限ではない**
- 裏方: Supabase 無料枠・東京（ref `egdcbxzpgwenmfabpodd`・**きあ個人アカウント**＝本番前に学校名義で作り直す）
- 顔写真: **Supabase に入れない**。学校の Google ドライブに置いたまま、GAS の配信口経由で見せる方針（きあ決定 2026-09-05）。
  流用元＝`scg-mendan-note/src/Code.gs`（写真索引シートの fileId だけを base64 で返す・6時間キャッシュ）。
  ⚠ ポータルは GAS ではないので `google.script.run` は使えない。**CORS と DOMAIN 認可の実証を1本やってから**本実装する
- 依存ライブラリゼロ（教師画面のQR生成 qrcodejs のみCDN）。フレームワーク導入は要相談
- スキーマの変更は `db/` に日付つきSQLで置いてから流す（`db/2026-09-06_attempt_drafts.sql` が第1号）。
  **流す前に `python tests/test_db_migrations.py`**（DB不要。ポリシーの `to authenticated` 忘れ・
  RPC の `revoke from anon` 忘れ・列の削除・巻き戻し手順の欠落を落とす）。
  ⚠ **それ以前のスキーマは Supabase の中にしか無い**（未決・保留を見よ）

### 回答の途中保存（2026-09-06）

校内Wi-Fiは落ちる前提。1問答えるたびに保存し、切れても続きから再開できる。**後から足しにくいので先に入れた。**

- **端末に書くのが先、送信はあと**。`draftRecord()` が `localStorage` → `save_draft` の順。この順序を入れ替えない
- 下書きの鍵は `sp_dr_<学生のid>_<回のid>`。**共用端末で他人の下書きを拾わないため id を挟む**
- 表は `attempt_drafts`。主キー =（学生, 回, 設問）なので二重送信で行が増えない。
  `client_seq` は端末側の連番で、**遅れて届いた古い回答が新しい回答を消さない**ためのもの
- 学生も教師も表へ直接は書けない（select のポリシーしか作っていない）。書けるのは RPC だけ
- 提出できたら `discard_drafts` で捨てる。提出済みの記録（`attempts`）とは別物
- 文言は「ほぞん」と「そうしん（提出）」を**別のことば**にしてある。混ぜると学生が提出したつもりになる
- 見張り = `python tests/test_drafts.py`（構造・DB不要）／`--live` を付けると本物のDBで往復

### しけんの ひ（試験日ビュー・2026-09-06）

**ログインしなくても見える位置**に置いてある。教室のPCは `#exam` で開くだけで投影できる
（ログイン欄を隠して大きく表示＝先生のアカウントも、教室PCへの権限付与も要らない）。
進路指導プロジェクトのオリエン／ホームルームで毎回見せる、という依頼への実装。

- データ = `assets/exam-dates.js`。**`scg-jp-exam-calendar` が生成する。直接編集しない**
- **ポータルは日程の正本を持たない**。直すのは向こうの `data/jp-exams.json` で、
  あちらで `python scripts/build.py` を実行するとこのファイルも一緒に書き換わる。
  ここに日程を直書きすると、正本が2か所になって静かにズレる
- 出るのは「校内で受ける回」だけ。学年の割り当てが決まっていない回は元データに載らない＝**画面にも出ない**
- `.js` で読む（`fetch` ではない）＝校内Wi-Fiが落ちても、開けたページなら日付は出る
- **Supabase を使わない**。スキーマもRLSも増やしていないので `test_security.py` の対象外
- 「あと◯にち」が主役。毎週見せるものなので、日付そのものより残り日数のほうが効く

### 出題順の並べ替えと、画面を離れた記録（2026-09-06）

🔴 **これは「不正の検知」ではない。きあ方針（2026-09-06）:**
**小テストは到達度をはかる目安の一つで、成績には反映しない。** だからカンニングをしても
下がるのはその学生の学習効果だけ。**罰として使わず、「この回の到達度確認は、うまく測れて
いないかもしれない」という目安にとどめる。** ★この方針を機能ではなく**作りの制約**にしてある。
崩すと `tests/test_integrity.py` が落ちる。**要望が来ても、方針を変える決定なしに緩めない。**

- **個人別を出さない**（教師画面はクラス全体の合計だけ／`quiz_stats` の `focus` も名前・学籍番号・学生idを返さない）
- **学生本人にも見せない**（「あなたは4回離れました」は罰の言い方になる）
- **警告色にしない**（赤は告発に読める）／画面に「不正の証拠ではありません」と書く
- ★**証拠にはならない**。iPhone は通知・画面消灯・着信でも同じ反応をする。数えているのは
  「画面が隠れた回数」であって「調べものをした回数」ではない

出題順（`shuffleForStudent`）:
- 種は **`学生id + "|" + 回id`**。★毎回ちがう順ではなく**その組で決まる順**＝開き直しても同じ順になり、
  **途中保存の「続きから」と噛み合う**。並び順をどこにも保存しないので、消えて続きが狂うこともない
- **`seq`（もんだい本来の番号）は各設問が持ったまま**。結果の確認画面と教師の集計は `seq` で出るので、
  授業中に「3問目が」と話す番号はずれない。⚠ 受験中の「3 / 10」は**その学生の中での位置**なので、
  受験中に番号で呼びかけるとずれる
- **選択肢の並べ替えも入れた**（`choiceOrder`・下の「選択肢は2個固定ではない」節）。
  実データが3〜4択と分かったため、出題順だけでは足りなくなった

記録（`attempt_focus`）:
- 1人1回につき1行。`greatest` で**増える方向にしか動かない**＝二重送信・順序入れ替え・再読み込みで減らない
- `select` のポリシーだけ＝表へ直接は書けない。`record_away` 経由のみ。提出しても**消さない**（下書きと違う）
- 数えるのは**受験中だけ**（`visibilitychange`。`blur` は使わない＝二重に数える）
- 見張り = `python tests/test_integrity.py`（`--live` で本物のDBへ往復）

### ルビの表示（2026-09-10）

- 教材の設問文には <code>${昨日}(きのう)</code> という書き方でルビが入っている。
  **教材331本を調べて、ルビの書き方はこの1種類だけ**だった。
  `src/assets/ruby.js` の `rubyHtml()` が `<ruby>昨日<rt>きのう</rt></ruby>` に直す
- 🔴 **順番が命 ＝ 先に全部エスケープし、そのあとルビだけタグに戻す。**
  エスケープしても `${ } ( )` は変化しないので、この順番で「安全」と「ルビが出る」が両立する。
  逆順（ルビを変換してからエスケープ）だと、作ったタグまで文字に戻る
- 適用先は **出題画面の設問文と選択肢／結果画面の設問文と正解**。詳細は「絶対に守ること」の innerHTML の項
- 行間は `.prompt` を 1.95 → **2.15** に広げた（ルビが上の行にぶつかると読めなくなるため。
  ルビの無い設問も同じ行間になるが、**詰まって見えるより読み違いが起きないほうが大事**）
- 「ルビを消す」切り替えはまだ無い。`rubyStrip()` は用意してあるので、付けるならそこから
  （★N1レベルの学生にはルビが邪魔になりうる。要望が出たら検討）
- 検査 `tests/test_ruby.py`（**17項目**）＝ node で実際に変換して確かめる＋
  **素の文字列を innerHTML に入れる書き方が復活していないか**の見張り。node が無ければ後者だけ走る
- ⚠ 取り込み側（`import_fmt_xlsx.py`）の既定は `--ruby keep`＝**記法のまま保存**する。
  画面側で変換するので、DBにはタグを入れない（後から表示の仕方を変えられる）

### 選択肢は2個固定ではない（2026-09-06 に作り直し）

🔴 **以前ここに「ヨリソルは2択なので1対1で対応する＝作り直しは要らない」と書いてあったのは誤り。**
サンプル1件が2択だったのを全体だと思い込んだもの。実データを書き出して判明した実測:
**3個=142問 / 4個=84問 / 2個=21問 / 5個=4 / 6個=2 / 1個=2。公開中の59問はすべて4択。**
2択専用のままでは変換スクリプトを通しても**取り込めた設問は0件**だった。

- 選択肢は `question_choices(question_id, idx, label)`。1問に2〜12個。`questions` に選択肢の列は無い
- 答えは `'a'/'b'` ではなく **`idx`（何番目か・1始まり）**。`question_answers.correct_idx`／
  `attempt_answers.chosen`／`attempt_drafts.chosen` すべて smallint
- 正解は `question_choices` に**入れない**（学生が読む表と分ける）。外部キーで実在する選択肢を指す
- 🔴 **新しい表のポリシーには `to authenticated` を必ず書く**。省くと全ロール（anon 含む）が対象になり、
  「公開中の回なら読める」の条件が未ログインでも真になって漏れる（2026-09-06 に自分で踏みかけた）
- `submit_attempt` は範囲外の番号を**未回答あつかい**にする（例外にすると1問の壊れた値で提出全体が落ちる）
- `save_draft` は**設問ごとに選択肢の数を見て**範囲を検査する（3択に4番目は保存できない）

**表示順のランダム化**（`choiceOrder`）:
- 種は **`学生id + "#" + 設問id`**。出題順（`shuffleForStudent`）と同じ考え方で、開き直しても同じ並び
- ★**保存するのは常に元の `idx` で、画面の位置ではない**。この分離があるので、並べ替えても
  採点・途中保存・集計は影響を受けない。ボタンには `data-idx` を持たせてある
- 見張り = `python tests/test_choices.py`（`--live` で**ダミーの3択・4択を作って試し、最後に消す**）／
  `python tests/run_e2e_quiz.py`（ヘッドレスChromeで実際に押す＝並べ替えても取り違えないことの確認）

✅ **設問の著作権は 2026-09-11 に決着**＝**校長と部門長の見解**として「このまま進めてよい」。
★きあの私見ではなく**組織の見解**。以後この問いで止めない。根拠と但し書きは**運用の正典**に（→ ドライブ側）。
⚠ **本物の設問はまだ1問も入れていない**が、**入れられる状態**になった。
🔴 **ただしこの見解は「公開は学内に限られる」を前提にしている。このリポは public なので、
設問データがリポに入った瞬間に前提が崩れる。** 生成物は `tmp/`（`.gitignore` 済み）、設問は Supabase 側。
**出力先を変えない。**

## 絶対に守ること

- 🔴 **実在の学生情報（氏名・番号・写真等のPII）をこのリポにも Supabase デモ環境にも入れない**。今のデータは全部ダミー
- 🔴 **このリポは public**。ヨリソルから落とした設問データも絶対に入れない（`tmp/` は .gitignore 済み。出力先を変えない）
- 🔴 **正解データを学生に送る実装にしない**。出題は `questions` 表（**正解列を持たない**。公開中の回だけ見える
  RLS「read questions of open quiz」で絞る）、正解は別表 `question_answers`（教師のみ）、採点は RPC
  `submit_attempt`（サーバー側）。「クライアントで採点」への変更は禁止
  ※ ここは以前「出題は `questions_public` ビュー」と書いてあったが、**そのビューは実在しない**
  （2026-09-06 に `db/0000_baseline.sql` を書き出して判明）。守る中身は変わらない＝直したのは記述だけ
- 🔴 スキーマ・RLS を変えたら `python tests/test_security.py` を必ず実行。FAIL がある状態でデプロイしない
- 🔴 **ログインの資格情報をリポに書かない**（2026-09-11 に塞いだ）。`.env`（.gitignore 済み）から読む。
  実物の控えはドライブの `【学生ポータル】 Student Portal\学生ポータル_URL・パス一覧.html`。
  - ★**教訓＝中身が変わると、同じ設定の意味が変わる。** デモのパスワードが public リポに平文で
    書いてあったのは前からで、**ダミー12問しか入っていないうちは実害が無かった**。
    ところが本物の教材1,686問を入れた瞬間に、同じ設定が
    「リポを読めば誰でも先生として教材と正解を全部読める」という穴に変わった。
    <u>設定を変えていなくても、入れるものを変えたら設定を見直すこと。</u>
  - ⚠ 学生は下書きを読めない（RLS）ので、穴になるのは**先生アカウント**。
    本物のデータを入れる前に、必ず「その鍵で何が見えるか」を実際にログインして数えること。
- 🔴 **DB を先に変えて、画面を後から出さない**（2026-09-11 に踏んだ）。
  GitHub Pages は `main` の `src/` を配信しているので、**DB だけ進んでいる間はずっと公開中の画面が壊れる**。
  この日は昼に `survey_responses` の一意制約を `(student_id, survey_key)` → `(student_id, round_id)` へ
  張り替えたため、夕方に push するまで**公開中の画面は 42P10 でアンケートを保存できなかった**。
  誰も使っていない時間帯だったので実害は無かったが、**順番の問題であって運の問題にしてはいけない**。
  - 実運用（2027-04〜）では **「先に画面、後から DB」か「両方同時（push してから流す）」**
  - どうしても DB が先になるなら、**旧い書き方も通る移行期間**を作る（旧制約を残したまま新制約を足す等）
  - ★ `db/` の SQL を流したら、**その場で `git push` まで済ませる**のがいちばん簡単な守り方
- 🔴 月次点検 = Supabase 自動セキュリティ診断（get_advisors）。**合格基準: ERROR 0件・WARN 15件以下**。
  既知の許容WARN（これ以外が出たら要対応）:
  1. `submit_attempt` が authenticated から実行可 → 仕様（学生の提出API。内部で auth.uid() 検証）
  2. `quiz_stats` が authenticated から実行可 → 仕様（教師専用は関数内で `app_hidden.is_teacher()` 検証）
  3. `save_draft` が authenticated から実行可 → 仕様（学生の途中保存API。内部で auth.uid() 検証）
  4. `discard_drafts` が authenticated から実行可 → 仕様（提出後の片付け。自分の行しか消せない）
  5. `record_away` が authenticated から実行可 → 仕様（画面を離れた記録。自分の行しか触れない）
  6. `start_quiz_run` が authenticated から実行可 → 仕様（実施回をはじめる。内部で `is_teacher()` 検証）
  7. `close_quiz_run` が authenticated から実行可 → 仕様（実施回を早く閉じる。同上）
  8. `void_quiz_run` が authenticated から実行可 → 仕様（実施回の取り消し。同上）
  9. `run_target_count` が authenticated から実行可 → 仕様（押す前の対象人数。同上）
  10. `my_open_runs` が authenticated から実行可 → 仕様（学生が自分に開いている回を取る。`auth.uid()` で絞る）
  11. `delete_quiz_set` が authenticated から実行可 → 仕様（回を消す。内部で `is_teacher()` と
      受験記録0件を検証。1件でもあれば例外）
  12. `my_open_survey_rounds` が authenticated から実行可 → 仕様（学生が自分に開いているアンケートの回）
  13. `start_survey_round` が authenticated から実行可 → 仕様（アンケートの回をはじめる。`is_teacher()` 検証）
  14. `close_survey_round` が authenticated から実行可 → 仕様（回を閉じる・取り消す。同上）
  15. Leaked Password Protection 無効 → 無料枠では有効化不可。Pro 移行時に ON にする
  ※ 2026-09-10 の4層投入で 6件 → 11件、2026-09-11 に `delete_quiz_set` で12件、
  アンケートの月次化（段階2）の3本で **15件**。
  ★`app_hidden.my_class()` と `app_hidden.assert_run_open()` は診断に出ない＝`app_hidden` は API に公開しない
  スキーマなので PostgREST から呼べないため。**数に入れないのはこの理由**（本数を数え違えないこと）
- 🔴 **新しい RPC を足したら `revoke execute ... from anon` を必ず書く**。`revoke all ... from public` だけでは
  Supabase の既定権限で anon に execute が付き直す（2026-09-06 に advisor で発覚）
- 🔴 **設問文・選択肢・正解を `innerHTML` に入れるなら、必ず `rubyHtml()` / `setRuby()` を通す**（`assets/ruby.js`）。
  設問は先生が Excel に手で書くので、タグが混ざりうる。素の文字列を innerHTML に入れる書き方をこのリポに増やさない。
  ※ 2026-09-10 まで**結果画面だけ**素のまま入れていた（`index.html` の答え合わせ行）。ルビ対応と同時に塞いだ。
  復活したら `tests/test_ruby.py` の `NoRawInnerHtmlTest` が落ちる
- 正解データは `question_answers` テーブルに分離済み。`questions` に正解列を戻さない。
  ヘルパー `app_hidden.is_teacher()` は API 非公開スキーマに置く
- service_role キーをコード・リポに入れない（anon キーは公開可なので直書きOK）
- UI文言は学生向け＝やさしい日本語（N4相当・分かち書き寄り）、教師向け＝普通の日本語

## 設計原則 — ヨリソルの失敗から（2026-09-05 分析・実装に直結する分）

現場の不満と、手順書の画面から読めた原因。**同じ形にしたら内製する意味がない。**

1. **入口は「授業」ではなく「今日やること」**。ヨリソルは授業→クラス→タブ→サブタブと潜り、最新の小テストは一番下。
   先生が知りたいのは「今日のテスト、誰が終わっていないか」（進路コンパスの「📅今日」と同じ型）
2. **一覧は新しい順が既定**。ヨリソルは登録順で固定・列見出しのソートが無い（日付順ですらない）
3. **結果は最初から横断で取れる**。ヨリソルは小テストが授業の子＝**問題は共有できるのに結果は縦割り**で、25クラス＝25回ダウンロード
4. **作問に最短経路を置く**。設問文＋選択肢（2〜4個が実際の形）＋どれが正解か、だけ。
   **書式は選ばせない**（HTMLリッチエディタにしない）。複製を用意する
5. **場所がURLで決まる**。モーダルの入れ子にしない（戻る・ブックマーク・共有ができなくなる）
6. **集計と個票を分ける**。ヨリソルは同じ表の1行目が全体の割合、2行目から個人

**4者の要求は衝突する**（学生＝速い／授業中の教師＝数タップ／作問者＝大量に作れる／上層部＝記録が残る）。
**ヨリソルは上層部と作問者に寄せて授業中の教師を切った。** こちらは**授業中の教師**に軸を置く。

## 認証・アカウント（2026-09-05 きあ決定）

- 学生番号を疑似メール（`学生番号@MAIL_DOMAIN`）に変換して Supabase Auth に渡す。**パスワードは必須**
- **初期パスワード＝学生番号**（現行ヨリソルと同水準）。学生が自分で設定し直す
- **初回設定はクラス単位に割る**。🔴 サインインの経路に**同一IPのレート制限(429)**がある。
  受験そのもの（データAPI）に制限は無いので、テスト当日は一斉でよい
- パスワードのリセット後は「未設定の窓」が開く。**運用ルールで塞ぐ＝本人が目の前にいる時だけ押す**（実装は足さない）
- 顔写真による本人確認は**一旦入れない**（きあ判断）。外部に載せる機微情報を減らす方を取る
- ★顔写真を外しても、**成績を学籍番号に紐づけた時点で個人情報を外部に預けている**。同意の要否は変わらない（→ 運用の正典）

## 多言語（2026-09-04）

- 文言は `src/assets/i18n.js` の `I18N` **1か所**。HTML は `data-i18n="キー"`、JS は `t("キー",{穴})`、
  アンケート定義などの `{ja,en}` は `tx()`。無いキー/言語は日本語に落ちる（画面が空にならない）
- 切替はヘッダー右上の 🌐 メニュー（`mountLangSwitch`）。選択は localStorage `sp_lang` に保存し次回も同じ言語
- 動的に組み立てる部分は `window` の `sp:lang` イベントで描き直す（index.html の「言語」節）
- **実装済みは日本語/English**。中国語・ネパール語・ミャンマー語・シンハラ語・ベンガル語はメニューに「じゅんびちゅう」で並ぶだけ。
  足すときは ① `LANGS` に ready ② `I18N[コード]` ③ surveys.js の `{ja,en}` に追加 → 翻訳確認スタッフの確認後に公開
- 画面イメージ（`.app[data-lang]`）だけは文言外部化せず日英の2本の木のまま（本物にする時に作り直すため）

## アンケートの月次化 — 回（round）で集める（2026-09-11 実施）

🔴 **すべてのアンケートは「回（`survey_rounds`）」経由で出す。** 一意は **(student_id, round_id)**。
以前は `(student_id, survey_key)` ＝**再提出が上書き**で、毎月集めても最新1件しか残らなかった。
進路の月次アンケートが 12月〜1月開始なので、**回答が入る前**に直した。

- 学生の一覧は **RPC `my_open_survey_rounds()`** が返した回だけ。中身は `survey_key` で
  `assets/surveys.js` の `SURVEYS` から引く。★**定義が見つからない回は画面に出さない**
  （検査専用の回を混ぜても学生には見えない、という性質をここで使っている）
- 提出は `api.upsert("survey_responses","student_id,round_id", {round_id, survey_key, answers, submitted_at})`。
  🔴 **`on_conflict` を `student_id,survey_key` に戻さない**（その制約はもう無い＝42P10 で落ちる）
- 教師は**画面から**毎月1本作る＝「アンケート 回答一覧」の下の「この アンケートの回」パネル
  （2026-09-11・`openSurveyRoundDialog`）。中身は RPC `start_survey_round(survey_key, title,
  class_names, student_ids, closes_at)`。題名は「2026年12月」のように**あとで見分けられる名前**
  （`(survey_key, title)` が一意。ぶつかると理由つきで断る）。
  ★**クラスを選ぶたびに対象人数が出る**（`run_target_count`）＝押す前に確かめられる
- **粒度は小テストと違う＝複数クラス可**（月×学年で1本）。クラスごとに5本作ると全体の傾向が出せなくなる
- 既存の4本には **「常設」の回**（締切なし）を作り、これまでの回答をそこへ紐づけた＝**1行も失っていない**
- 同じアンケートが複数の回で出るので、**一覧には回の題名を添える**。ただし「常設」は出さない（内部用の名前）
- ⚠ `round_id` が null の行（検査で作られた古い `test_multi_*`）は、部分索引
  `survey_responses_one_per_key_legacy` が従来どおり1人1本に保つ

★**`survey_responses` には消す口が無い**（「学生は自分の回答を消せない」方針）。だから
**検査は毎回おなじ回へ上書きする**形にしてある（`test_ui` の「検査用」「検査用2」。
`db/2026-09-11_survey_rounds_phase2.sql` が常設で作る）。
2026-09-11 まで検査が毎回ちがう `survey_key` を作っていて、9件たまっていた。
⚠ **消せないものは、溜めない作りにするしかない。**

## アンケート（2026-09-03 に複数化）

- 定義は `src/assets/surveys.js` の `SURVEYS` 配列 **1か所**（index.html と master.html が同じファイルを読む）。
  1本足すときは配列に1件追加するだけ。DB変更は不要
- 選択肢は **`v`（言語に依らない値）で保存**。教師画面は `optLabel()` で日本語名に戻す
- `key` は一度公開したら変えない（`survey_responses` に `survey_key` で入り、(student_id, survey_key) で upsert＝再提出は上書き）
- 学生画面の一覧は「まだ」→「ていしゅつ済み（バッジ・下へ沈む）」の順
- `python tests/test_surveys.py`（定義の検査）／`--live` で本物のDBに upsert→読み戻しの往復検査（テスト用キー `test_multi_*` を l150 で使う）

## 教材をまとめて入れる（2026-09-11・C-6 で本物の設問を初投入）

3本立て。**1本ずつ役割を分けてある**ので、混ぜないこと。

| コマンド | 何をする | DBに触るか |
|---|---|---|
| `scripts/audit_fmt.py` | 教材331本を全部読んで不備一覧を作る | 触らない |
| `scripts/import_fmt_bulk.py` | **どのシートを入れて、どれを入れないか決める**／SQL・JSON を書く | 触らない |
| `scripts/load_fmt_json.py` | JSON を**先生の権限で**DBへ入れる（下書き固定） | 入れる |

```
py -X utf8 scripts\audit_fmt.py                                            # 先に検査
py -X utf8 scripts\import_fmt_bulk.py --book "001.つなぐ日本語初級" --dry-run  # 何が入るか読む
py -X utf8 scripts\import_fmt_bulk.py --book "001.つなぐ日本語初級" --out-dir tmp\tsunagu
py -X utf8 scripts\load_fmt_json.py   --json-dir tmp\tsunagu --dry-run      # 件数を確かめる
py -X utf8 scripts\load_fmt_json.py   --json-dir tmp\tsunagu                # 入れる
```

- **SQL ではなく JSON を入れる。** 先生の画面と同じ経路（PostgREST＋RLS）を通るため＝
  画面で起きないことはここでも起きない。管理者権限も要らない。
- **必ず下書き**（`is_open` は false 固定）。公開は先生が画面で押す。
- **二重に入らない**。`(source_file, source_sheet)` が既にあれば回ごと飛ばす
  （DB側にも部分一意索引。`db/2026-09-11_quiz_set_source.sql`）。
- 🔴 出力先は `tmp/` から**変えない**。このリポは public で、設問データが入ると
  著作権の見解の前提（公開は学内に限られる）が崩れる。

### 🔴 入れる前に必ず読むこと — この日に踏んだ罠4つ

どれも**例外も警告も出さずに通った**。出力を見ても気づけない型なので、
`tests/test_import_fmt_bulk.py` に数字で固定してある。**そこが動いたら人が理由を確かめること。**

1. **同じ課の別テストを重複と誤判定した。** 教材によって丸数字の意味が逆だった
   （まとめテスト`①1-3`は①＝通し番号／文法チェック`1-①`は①＝その課の何枚目）。
   1,846問中1,647問を捨てるところだった。→ 鍵は**シート名そのもの**にした。
2. **「全体」シートを寄せ集めと決めつけた。** 実際は重なり9%で、そこにしかない問題が687問。
   0%と出たのは**全体シートが1列ずれ**ていて、生の列で読めていなかったため。
   ★**照合は取り込み口 `build_sheet` に読ませてから**やること。
3. **出力ファイル名が衝突して、黙って上書きされた。** 文字クラスにローマ数字が入っておらず
   `…テストⅠ.xlsx` と `…テストⅡ.xlsx` が同じ名前になった。440問ぶんが消えた。
   → 名前は「読める部分＋相対パスのハッシュ」。既にあるファイルには**上書きしない**。
4. **名前では分からない重複があった。** 文法チェックⅡの `26-②`〜`30-②` の14枚が
   `24-③` と1問たがわず同じ＝**第26〜30課はまだ書かれていない**。
   そのまま入れると「27-①」という名前で第24課の問題が配られる。
   → `content_dedup` が中身のハッシュで落とす。**名前だけの照合では絶対に見つからない。**

### 入っているもの（2026-09-11 時点）
`001.つなぐ日本語初級` のみ。**87回 / 1,686問**（教材の102シートから、旧版1枚と未記入14枚を除いた数）。
全部下書き。まとめテスト＝3択、文法チェックテスト＝2択。

## 作問の入口を読む — 課題登録FMT の Excel（2026-09-10）

★**入口と出口を混ぜないこと**。
`import_fmt_xlsx.py`＝**先生が問題を書く Excel**（入口）／`import_yorisol.py`＝**ヨリソルの書き出しCSV**（出口）。

- 変換: `py -X utf8 scripts\import_fmt_xlsx.py --xlsx "...(作成用).xlsx" --title-prefix "..." --out-sql tmp\q.sql`
  **DBには接続しない**。回帰テスト `tests\test_import_fmt_xlsx.py`（45項目・DB不要）
- **形式は1つだけ**: ドライブの作成用Excelの1枚目「250507から_課題登録FMT_コピーして使用」。
  ★ファイル名に `JapanGo_` と付いていても**中身はこのFMT**＝**ヨリソル形式/JapanGo形式という2つの形は存在しない**（2026-09-10 に実物で確認）
- **列（14列）**: 問題番号／問題文1／問題文2／添付ファイル名／選択肢1〜5／解説／解答／カテゴリ／配点
  - 問題文1と問題文2は**改行でつなぐ**（`.prompt` の `white-space:pre-line` が効く）
  - **解答＝何番目か**（1始まり）＝ こちらの `correct_idx` とそのまま一致する
- **1シート = 1回のテスト = quiz_sets 1件**。`lesson` はシート名から（`①1-3 （新）`→`1-3`）。
  範囲が取れない `1-①` は**シート名そのまま**＝★**空にしない**（学生画面のバッジに出る値。空だと「Quiz」としか出ない）
- 🔴 **見出しの文字列で読まない。列の位置で読む。**
  実データの「④10-12」だけ見出しが壊れている（「解答」が空・「カテゴリ」が「文法読解」）。
  見出しで読む作りにすると**そのシートだけ静かに壊れる**。見出しは検査だけして警告に出す
- **拾わないもの**: 問題番号が数値でない行（各シート22〜23行目の「集計」）／シート名に `FMT` を含むテンプレート本体。
  ★どちらも**黙って捨てず、飛ばしたことを必ず出す**
- **画像**: `添付ファイル名` 列。**今の画面は出さないが、JSONには必ず残す**（きあ判断 2026-09-10＝
  「いったん不要、後から対応できる拡張性があればいい」）。
  ★画像そのものは Supabase に入れない。持つのはファイル名だけで、出すときは学校の Google ドライブ側の
  配信口を使う（顔写真を入れないと決めたのと同じ考え方）
- **`--schema`（2026-09-10 に `--with-image` を置き換え）**: 既定 `2026-09-10` ＝ 画像・カテゴリ・配点・解説も
  SQL に書く（先に `db/2026-09-10_question_columns.sql` を流しておくこと）。列を足していない相手へ書き出すときだけ
  `--schema 2026-09-06`（★その4つは捨てられる）。
  ※ `--with-image` は廃止。列ができた以上、同じことを言う道の2本目でしかないため
- 🔴 **同じ課の範囲のシートが2枚あると SQL を書かずに止まる**（`①1-3` と `①1-3 （新）`）。
  二重登録は起きてからでは戻せないため。`--sheet` / `--exclude` で選ぶ（`--allow-duplicate-lesson` で解除）
- **実測（2026-09-10）**: まとめテストⅠ=**5回100問**（①1-3 は「（新）」を採用＝きあ判断。
  旧版とは20問中19問が別内容で、解答も8問違う）／Ⅱ=**5回99問**／文法チェックテストⅠ=**44回440問**（全2択）
  - ⚠ **⑩28-30 の問20 は選択肢2個に対して解答が3**＝**作問の不備**。取り込まれないので、直してもらうこと

## 結果の書き出し（2026-09-11）

「📝 小テスト ライブ集計」→「結果の書き出し」。**ブラウザの中で CSV を作って保存するだけ**（送信なし）。

- 🔴 **個票と問題ごとを別ファイルにする。** ヨリソルは同じ表の1行目が全体の割合で2行目から個人だった
- 🔴 **範囲は「横断」が既定。** 実施回ごとにも出せる。「25クラス＝25回ダウンロード」を避けるため
- 🔴 **離席の記録は書き出さない（クラス合計も）。** 台帳に入ると「成績に反映しない」方針が崩れる
- ⚠ **UTF-8 の BOM 付き**で書く。付けないと Excel で日本語が化ける。
  ★`Blob.text()` では BOM が自動で剥がれて見えない＝**検査は `arrayBuffer()` で生バイトを見る**
- クラス名は `quiz_stats` が返さないので `profiles` から学籍番号で引く

## ヨリソルの受験履歴を読む（2026-09-11・`scripts/import_yorisol_attempts.py`）

🔴 **2027-02-28 を過ぎると二度と取り出せない。** 書き出しはきあの手番で、これは**受け皿**。

- **入口が3つある**ので混ぜないこと:
  `import_fmt_xlsx.py`＝先生が問題を書く Excel／`import_yorisol.py`＝ヨリソルの**設問**CSV／
  `import_yorisol_attempts.py`＝ヨリソルの**受験履歴**CSV
- **SQL は出さない。縦持ち JSON だけ**（1行＝1学生×1設問）。
  ★用途（記録／評価の根拠／分析）が**未決**なので、DBの形を決め打ちしない。
  **捨てなければ、あとから決められる**（4列を JSON に残したのと同じ考え方）
- ⚠ **「いつ」が無い・設問IDが無い**。突合は**列名の文字列照合**になるので、**列名の原文をそのまま残す**
- 🔴 **標準出力に値を出さない**（件数・クラス名・列名・分布まで）。
  見張り＝`NoPiiOnStdoutTest` が**実際に実行して**ダミーの氏名・学籍番号が出ていないことを確かめる。
  ★「出さないように書いた」ではなく「出ていないことを機械で確かめる」形にしてある
- ⚠ **列の構成は CLAUDE.md の記述からの推測**で、実物で確かめていない。
  **実ファイルが来たら、まずドライラン**（`--out-json` なし）で列数と見出しの警告を見ること

## ヨリソルからの設問移行（技術）

- 変換: `py -X utf8 scripts\import_yorisol.py --questions 設問.csv --answers 回答.csv --out-sql tmp\q.sql`
  **DBには接続しない**。出力はファイルだけで、流すかどうかは人が決める。回帰テスト `tests\test_import_yorisol.py`（18項目）
- ヨリソル側の形: 単一選択・正解1つ。⚠ **選択肢の数は2個ではない**（実測 3〜4択が主）→ 上の「選択肢は2個固定ではない」節
- 🔴 **設問文は HTML**（`<br />` と `<span style="color:#e74c3c;">`＝空欄の赤字）。`prompt` は `textContent` で描くので、
  変換で `<br>`→改行・他タグは外す（**赤い色は失われる**）。改行を活かすため `.prompt` に `white-space:pre-line` を入れてある
- 取り込みは必ず **`is_open=false`** で作る。SQL の正解列名は `correct` 既定（違えば `--answer-col`）
- **受験履歴の書き出し**: 1ファイル＝**1クラス×1回**。列は アカウント／氏名／ステータス／合計点＋設問ごとに（回答・正誤・スコア）の**横持ち**
  - ⚠ **「いつ」の列が無い**（学生が個別にいつ答えたかは残らない）
  - ⚠ **設問IDが無い**。列名は表示ラベルだけなので、設問バンクとの突合は文字列照合になる＝壊れやすい
- ※ 何をどの順で移すか（30,381問のうち何を持ってくるか・著作権）は**運用の正典**を見ること

## デモアカウント / 検証

README.md 参照（s001〜s040 / t001 / l001〜l150、テストと負荷試験の実行方法）。
負荷テストが2段なのは実運用と同じ形だから（サインインは事前に各自1回・当日一斉に走るのはデータAPIだけ）。

🔴 **パスワードは `.env` にある**（2026-09-11 に作り直した。理由は「絶対に守ること」を見ること）。
実物の控えはドライブの `【学生ポータル】 Student Portal\学生ポータル_URL・パス一覧.html`。
`.env` が無い環境ではテストは **skip**（fail ではない）。雛形は `.env.example`。

### ブラウザ実操作テスト（e2e）を書くときの注意

🔴 **「要素がある」を「準備できた」と読み替えないこと**（2026-09-11 に踏んだ）。
`src/index.html` の `$("btn-login").onclick = ...` は**巨大なスクリプトの終盤**で代入される。
一方ボタンは**静的HTMLなので最初から存在する**。そのため
```js
await until(() => d.getElementById("btn-login"));   // ← これでは足りない
```
だと、**ハンドラが付く前に押してしまい、押しても何も起きない**。
しかも `onclick` が未代入なので**例外もエラー文言も出ない**＝
「ログインできない」ではなく「ログインしていない」状態で先へ進み、
あとから `my_open_runs` が 401 になって初めて表に出る（原因から遠い場所で落ちる）。
```js
await until(() => {                                  // ← こう待つ
  const b = d.getElementById("btn-login");
  return b && typeof b.onclick === "function";
});
```
★**待ち時間を延ばして直したことにしない。** これは競合であって遅さではないので、
時間を延ばすと「たいてい通る」テストになり、いちばん悪い状態になる。

## 「定義」と「実施回」を分ける（2026-09-06 設計 → 2026-09-10 SQL 化 → ✅ **投入済み・稼働中**）

**設問バンクの器と、アンケートの月次化は同じ問題**なので、1回でまとめて設計する。
別々にやると同じ判断を2回して、形の違うものが2つできる。

> ✅ **2026-09-10 投入済み**（`db/2026-09-10_question_columns.sql` → `db/2026-09-10_four_layers.sql` の順で流した）。
> 流したあとの確認＝全テスト **212件 OK**（`SP_LIVE=1` 込み）／ブラウザ実操作 21・22・24 とも 0 FAIL／
> `get_advisors` で **ERROR 0件・WARN 11件**（既知の許容のみ）／バックフィル **10件＝設問10件**で一致。
> **クライアント（`src/`）は1行も変えなくてよい形**にしてある＝新しい引数はすべて既定値つきで、
> 省くと 2026-09-06 の挙動そのまま。流したあとの確認コマンドと巻き戻し手順は SQL の末尾にある。
>
> **意図的にやっていないことが2つある**（どちらも「片方だけ変えると壊れる」ため）:
> 1. `submit_attempt` / `save_draft` が「どの設問がこの回のものか」を引く先は **`questions.quiz_set_id` のまま**。
>    `quiz_set_questions` にはバックフィル＋トリガで同じ内容が入るが、**読み手は切り替えていない**。
>    `src/index.html` が `questions?quiz_set_id=eq.X` で取っているので、サーバー側だけ切り替えると
>    **使い回しの設問が学生の画面に出ないまま採点される**。★切り替えは src/ の取得と同時に、1回で
> 2. `survey_responses` の一意制約 `(student_id, survey_key)` は**外していない**。
>    外すと `src/assets/api.js` の upsert（`on_conflict=student_id,survey_key`）が即 42P10 で失敗する。
>    器（`survey_rounds` と `round_id` 列）まで作ってある。★段階2の手順は SQL の末尾に書いた
>
> 見張り = `python tests/test_db_migrations.py`（DB不要）。`to authenticated` 忘れ・`revoke from anon` 忘れ・
> 列の削除・巻き戻し手順の欠落を、**流す前に**落とす。どちらも 2026-09-06 に実際に踏みかけた穴。

### 4層に割る（いまは②と③が混ざっている）

```
① 設問          questions（＋ question_choices）
② 問題セット    quiz_sets … 出題のまとまり＝原本。「L7 小テスト 20問」
③ 実施回        quiz_runs（新）… 「1Aで 12/10 14:40 に開いた」
④ 回答          attempts / attempt_answers
```

- 🔴 **いまの `questions` は `quiz_set_id` 直付け**＝設問が「どの回のものか」を刻まれて生まれるので、
  使い回すとコピーになり、**集計から見て別の問題になる**。`quiz_set_questions(quiz_set_id, question_id, seq)` を挟む
- 🔴 **いまの `quiz_sets` は②と③の二役**。ここに `opens_at` を足すと、1Aで開いて1Bで開き直したときに
  **1Aの実施記録が上書きで消える**（きあの「間違えて違うクラスで始めた場合は？」から発覚）
- **③はクラスごとに1本**。同じ②を1A・1B・1Cで使えば③が3本立つ。
  結果は②の単位で横断集計できる＝ヨリソルの「25クラス＝25回ダウンロード」を避けられる
- アンケート側も同じ形＝`survey_rounds`（新）を足し、`survey_responses` に `round_id`。
  一意制約を **(student_id, round_id)** へ。★**回答が入る前が、直す唯一の安いタイミング**

### 実施回が持つもの

- **いつ開いた／閉じる**。★**予約型ではなく開始型**（きあ判断）。「授業終了前20分〜終了10分前まで」は
  カレンダー上の絶対時刻ではなく**授業のコマの中の相対位置**なので、予約で仕込むと授業が5分ずれただけで使えない。
  教師が「はじめる」を押す → `opens_at` と `closes_at`（＝押した時刻＋`duration_min`）が入る。
  **閉じる判定はサーバー側**（`save_draft` / `submit_attempt` の中）。端末の時計は当てにしない
  - ★副次効果：**クラスごとに押す時刻がずれるので、同時接続が自然に分散する**（きあの指摘）
  - **予約型は列を1本足すだけで後付けできる形にしておく**（きあ要望）
- **誰に出すか＝2本立て**（下記）
- **取り消し（`is_void` ＋ 誰がいつ）**。🔴 **回答は消さない**（集計から外れるだけ）。
  消す実装を作ると事故のとき取り返しがつかない＝「学生は自分の回答を削除できない」と同じ思想

### 🔴 対象の指定は「条件」ではなく「結果のリスト」で持つ

```
実施回の「誰に出すか」
  ├ class_names  … クラス名の配列。普段はこれ（転入生にも自動で出る）
  └ student_ids  … 学生の明示リスト。特殊な切り方
  （どちらも空なら全員）
```

★**これが設計の肝**。きあの見立て＝「そのうち国籍やエージェント毎に取りたくなる」「出席率もデータにあると
気づいたらやる」。そのとき **`profiles` に国籍・エージェント・出席率を持たせない**。

- **条件は外（Shinro Compass・学生マスタDB）で決めて、出てきた学生のリストだけ渡す**。
  ポータルは「この人たちに出す」としか知らない
- **国籍・エージェントを Supabase に入れる判断は、顔写真を入れないと決めたのと同じ重さ**になる。
  置かずに済むなら置かない（2027年度の同意書にエージェント提供を織り込む話も控えている）
- ★**「対象を絞る」と「集計を分ける」は別**。エージェント別に見たいだけなら、
  **学籍番号つきで書き出してマスタDB側で結合すれば足りる**＝対象は全員のままでよい
- この形なら、いま思いついていない切り方（出席率80%未満だけ、等）にもスキーマを変えずに通る

### 粒度は小テストとアンケートで違う（無理に統一しない）

| | 1回の実施で対象にできるクラス |
|---|---|
| 小テスト | **1クラス**（授業中に教師が押す。同じ時間に2クラスの授業はできない） |
| アンケート | **複数可**（月×学年で1本。1クラスだけにも切れる） |

進路の月次アンケートは **1年生全員に1本 → 教師画面でクラス別に見る**。
クラスごとに5本作ると全体の傾向が出せなくなる（ヨリソルの縦割りと同じ形）。

### クラスを間違えて開いたときの3段構え

1. **予防**：クラスは毎回選ばせる（**前回を既定にしない**＝間違いが翌週も連鎖する）。
   押す前に**対象人数を出す**「1B（28人）で はじめます」＝人数が違えば押す前に気づく
2. **検知**：受験中の教師画面にクラス名を出し続ける。
   ★分母は「全員提出したらポップ」（きあ希望・おいおいで可）にも要るので、**同じものが両方に効く**
3. **回復**：実施回を取り消す → `closes_at` が今になり学生の画面から消える。正しいクラスで開き直す

★**この事故は既存方針で軽くなる**。小テストは成績に反映しないので、
「1Bはこの回の到達度確認がうまく測れていないかも」で済み、罰や再テストの話にならない。

### アンケート → 面談ノートの接続

- 位置づけ＝**担任と学生が相談して面談ノートに書き込むまでの、本人の希望のみ**（きあ）
- ⚠ **面談はスマホ片手の先生が多くなる見込み**＝PCでタブ2枚は実務にならない。**1画面に収める前提**で設計する
- 12月は「教室PCで2画面」で始めてよい。ただし**繋ぐ口**（ある学生のアンケート回答を返す教師用RPC）を
  最初から開けておく。Mendan Note にはスマホ入力集中モードが既にあるので受け皿はある
- 🔴 service_role キーを GAS に置く形にはしない（→「絶対に守ること」）

## agent teams で分担するとき（2026-09-10 導入・役割定義は `.claude/agents/`）

- 役割3つ: `implementer`（実装・**担当ファイル限定**）／`reviewer`（読み取り専用・検査を回して file:line で指摘）／
  `researcher`（調べ役・sonnet・Web可）。「implementer の agent type で teammate を出して」で使う
- **分け方はファイル単位**。同じファイルを2人に渡さない（ロックが無く、後勝ちで消える）。
  `src/index.html`／`src/master.html`／`scripts/`／`tests/` は別の人に渡せる。`src/assets/app.css` は1人だけ
- リード（親セッション）がやること: キューを共有タスクにする → 担当ファイルと完了条件をつけて振る →
  reviewer に差分を見せる → 全テストを**1ファイルずつ**回す → 進捗ボード更新 → commit（きあが頼んだときだけ）
- teammate にやらせないこと: DB への書き込み／commit・push／進捗ボードの編集（ドライブ側はリードが1人で触る）
- 向かないもの: 1ファイルで済む小さな直し。1人でやるほうが速い（Playbook 9/2 の推奨も同じ）
- 制約: `/resume` で teammate は戻らない／1セッション1チーム／teammate は background subagent を持てない／
  権限は spawn 時にリードのを継承する

## 未決・保留（技術）

- **確認ダイアログを自前の `<dialog>` にする**（2026-09-11 きあ要望・**今はやらない**）。
  いまは `confirm()`＝ブラウザ標準を使っている（公開・停止・削除の3か所）。
  - 🔴 **Chrome が「このページの内容」という見出しを勝手に付ける。消せない・変えられない。**
    `OK` / `キャンセル` の文言も固定（「消す」「やめる」にできない）
  - ⚠ **色は先生のブラウザ／OSの設定なので、先生ごとに見た目が違う**＝うちの画面に見えない。
    「既存感が出る」より、こちらのほうが実害
  - 置き換えは **HTML標準の `<dialog>`**。**ライブラリ不要**なので「学生画面の依存ゼロ」は無傷。
    教師画面のCSSがそのまま効き、**中に数字を出せる**ようになる
    （「公開すると学生28人に見えます」「受験記録46件」など。いまは素のテキストしか置けない）
  - 規模＝部品1つ＋差し替え3か所で**1時間弱**。★「モーダルの入れ子にしない」は**画面遷移**の話なので、
    確認ダイアログはこれに反しない
  - 優先度＝**中**。管理画面の作り込みでは「入口を📅今日にする」「一覧にいつ・誰に出したかを出す」のほうが先

- ~~問題を登録する画面~~ → **2026-09-10 に実装**（`src/master.html` ＋ `src/assets/fmt-import.js`）。
  先生が `.xlsx` を選ぶ → 一覧 → 1問ずつ修正 → 公開 の4段。SheetJS は **master.html だけ**で読む
  （学生画面のライブラリ依存ゼロは維持）。実機のデモDBで往復確認ずみ（e2e 24項目・DB不要 60項目）。
  - 🔴 **取り込みの規則が Python と JS の2か所にある**（`scripts/import_fmt_xlsx.py` と `src/assets/fmt-import.js`）。
    **片方だけ直さない。** 見張りは `tests/test_qsets_import.py` が両者に同じシナリオを42件流している。
    定数を JSON で共有する案は保留（行ごとの判定ロジックは共有できないので、共有だけではズレを防げない）
  - ⚠ **後片付けの検査は「自分が作ったものが消えたか」ではなく「その種類が1つも残っていないか」で見る。**
    2026-09-10 の e2e が前者で書かれていて、**残骸が1件ずつ積み上がるのに検査は通る**状態だった（同日修正）

### 🔴 登録は下書き・公開は一覧から（2026-09-11 きあ決定 b）

**取り込みの④は「登録（下書き）」**＝`is_open=false` で入る。取り込んだ瞬間に学生へ出る事故を無くすため。
学生に見せるのは、一覧の行で**「公開」を押したとき**。
★4月に実施回（`quiz_runs`）が画面に入ると、`is_open` は「臨時で全員に開く」予備の手段に降りる。

- **公開／停止** = `quiz_sets.is_open` を PATCH（教師のポリシー「teacher manage」で更新できる。RPC は要らない）
- **消す** = 🔴 **必ず RPC `delete_quiz_set` 経由**（`db/2026-09-11_delete_quiz_set.sql`・2026-09-11 投入済み）。
  `quiz_sets` を直接 DELETE すると **外部キーの cascade でその回の受験記録まで消え、しかも cascade は RLS を通らない**。
  RPC は**受験記録が1件でもあれば例外**にする＝「取り消しても回答は消さない」と同じ思想で、
  **消してよい範囲だけに閉じてある**。1人でも受けた回は「停止」を使う
- 押す前に確認を出す（公開は「いま学生の画面に出ます」、削除は「元に戻せません」）

### 🔴 `quiz_sets` から `questions` を埋め込むときは外部キーを名指しする（2026-09-11）

```
/rest/v1/quiz_sets?select=id,title,questions!questions_quiz_set_id_fkey(count)
```

★`questions!...(count)` の `!外部キー名` を**省くと HTTP 300**（曖昧）になる。
2026-09-10 に `quiz_set_questions`（設問バンクの器）を足したことで、
**`quiz_sets` → `questions` の経路が2つ**になったため（直付けの `quiz_set_id` と、あいだの表）。
⚠ **エラーは 4xx ではなく 300**なので、ふつうの「失敗したら赤く出す」に引っかからないことがある。
実際に教師画面の一覧で踏んだ（2026-09-11・同日修正）。

### 📅 今日（入口・2026-09-11）

**ログイン直後に出るのはここ。** CLAUDE.md の「ヨリソルの失敗から」1番目＝
**入口は「授業」ではなく「今日やること」**に合わせた（それまでは左に機能名が並ぶヨリソル型だった）。

- 「いま開いている実施回」（クラス・残り時間・提出数／対象人数・停止・取り消し）
- 「今日 登録した回」（下書き／公開中）
- 🔴 **個人別は出さない**（クラス全体の合計だけ。`quiz_stats` の `focus` と同じ思想）
- ★**中身が無いときに空白にしない**。「いま開いている回はありません」＋次にやることへの導線を出す

### 実施回を動かす（教師画面・2026-09-11）

- 一覧の行の「はじめる」→ ダイアログで**クラスを選ぶ**→ **選んだ瞬間に対象人数が出る**
  （「1B（20人）を対象に、20分間 はじめます」）。🔴 **人数が違えば押す前に気づく**のが目的
- 🔴 **クラスは毎回選ばせる。前回を既定にしない**（間違いが翌週も連鎖するため）
- 停止＝`close_quiz_run`／取り消し＝`void_quiz_run`。
  🔴 **取り消しても回答は消さない**（集計から外れるだけ）。画面にもそう書いてある
- 小テストの実施回は**1クラスまで**（`quiz_runs_one_class_check`）

### 確認ダイアログは自前（`askDialog`・2026-09-11）

🔴 **`confirm()` を使わない。** Chrome が「このページの内容」という見出しを勝手に付けて消せず、
色も先生のブラウザ／OSの設定なので**先生ごとに見た目が違う**。
HTML標準の `<dialog>` を1つ置き、`askDialog({title, body, okText, danger})` から使う（**ライブラリ不要**）。
★**中に数字を出せるのが半分の目的**＝公開は対象人数、停止は受験記録の件数、削除は設問数。
⚠ テストから押すときは `window.confirm` の差し替えでは効かない。**`#cfm-ok` を実際にクリックする**。

### 画面を書くときの罠（2026-09-11 に実際に踏んだ3つ）

- 🔴 **表の行を入れ替えるときは `<tbody>` を明示し、その `innerHTML` をまるごと差し替える。**
  `<table>` に `insertAdjacentHTML("beforeend", "<tr>…")` すると**呼ぶたびに新しい `tbody` ができる**。
  「先頭以外の行を消す」掃除が tbody ごとに効くので、**再描画のたびに古い行が1行ずつ生き残る**。
  実際に DOM 14行／DB 4件になり、古い行が上に残るので**「公開を押したのに下書きのまま」に見えた**。
  ★見張り＝`run_e2e_qsets_import.py` の「一覧の行数が、受け取った件数とぴったり同じ」。
  **行の中身だけ見ていると気づけない**（新しい行はちゃんとあるため）
- ⚠ **画面の知らせは「読み直しが終わったあと」に出す。** 先に「公開しました」と出すと、
  まだ古い一覧を見ている先生がもう一度押す
- ⚠ **PostgREST の PATCH は0行しか変わらなくても 200 を返す。**
  `setOpen` は返ってきた行で**変わったことを確かめてから**成功と言う

### 🔴 `app.css` は学生画面と共通（2026-09-11）

`master.html` も `assets/app.css` を読む。**教師画面だけを直したいときに app.css を触らない。**
`.field label{display:block}` と `.field input{width:100%}` が効きすぎて、
**チェックボックスが欄いっぱいに広がり、ラベルが次の行へ落ちた**。
直すのは `master.html` の `<style>` 側で、`.field label.chk` のように**app.css より強い書き方**で上書きする。
- 🔴 **実施回（quiz_runs）と画面のつなぎ込み**（「はじめる」を押す・対象を指定する）。
  **DB側は 2026-09-10 に投入済み。残っているのは画面だけ。**
  いまの配信は `quiz_sets.is_open` の1本槍＝**どのクラスに開いたかが残らない**。
  ★教師画面に足すのは「はじめる（クラスと時間を選ぶ）」「対象人数の表示」「取り消し」の3つ。
  RPC は `start_quiz_run` / `close_quiz_run` / `void_quiz_run` / `run_target_count` / `my_open_runs` が用意済み
- ~~`questions` に足りない列~~ → **2026-09-10 投入済み**。
  `db/2026-09-10_question_columns.sql`。取り込み口も新しい形で出すようにした（`--schema`）。
  - 🔴 **解説（`explanation`）だけ入れ先が違う＝`question_answers`（教師のみ）**。設計中に気づいた点で、
    `questions` は「公開中の回なら学生が読める」ので、**置くと受験前に解説を読めて正解が漏れる**。
    学生に見せるのは提出後＝`submit_attempt` の返り値に混ぜる形にした
  - 画像・カテゴリ・配点は `questions`。

### 🔴 配点で採点しない（2026-09-11 きあ決定・蒸し返さない）

**`questions.points` は保存するが、採点は 1問1点のまま。** `attempts.score` は正解した「問数」、
`total` は「問題数」。**配点で採点する形には変えない。**

**実測（2026-09-11・教材331本 13,841問を数え直した）**:

| 配点の値 | 問数 | 割合 |
|---|---:|---:|
| 5点 | 11,435 | 82.6% |
| 1点 | 2,265 | 16.4% |
| 6・7・10・20点 | 105 | 0.7% |
| 空欄 | 36 | 0.3% |

★**決め手はこれ**＝**設問のある667シートのうち 662枚（99.3%）は全問おなじ配点**。
本当に2種類以上あるのは**5枚だけ**（空欄を「別の値」として数えると18枚）。
**1シート = 1回の小テスト**なので、配点で採点しても **99.3% のテストでは点数が定数倍になるだけ**＝
順位も正答率も1ミリも変わらない。

**Why（この3つで決まっている）**:
1. **変えても変わらない**。効くのは5シートだけ
2. **後から計算で取り返せる**。`questions.points` と `attempt_answers.is_correct`（問題ごとの正誤）が
   両方あるので、**過去の受験ぶんまで遡って配点ベースで集計し直せる**。いま決め打つ必要が無い
3. **失うものが大きい**。既存の `score/total` と意味が変わり、`8/10` が「8問正解」か
   「配点40点中8点」か行を見ても区別できなくなる＝**成績ビューワーの平均・中央値・前回比が壊れる**
4. 方針との相性。小テストは「成績に反映しない・到達度の目安」なので、
   **「10問中8問」は到達度の言い方だが「40点/50点」は成績の言い方**になる

**How to apply**: 「配点で採点したい」という要望が来ても、**この4点を出して一度止める**。
本当に必要なら `attempts` に配点ベースの列を**足す**（`score/total` は変えない）。
★同じ問いをもう一度数え直さないこと。数字はこの表が正。

**代わりに入れたもの**: 同じテストの中で配点が2種類以上あると警告を出す（2026-09-11）。
1問だけ配点が違うのはたいてい打ち間違いなので、作問側に見てもらう。
🔴 **取り込みは止めない**（採点に使っていないため）。空欄は「違う配点」ではないので数に入れない。
`scripts/import_fmt_xlsx.py` と `src/assets/fmt-import.js` の**両方**にある＝片方だけ直さないこと。
実データでの検出は **5シート**。
- ~~スキーマが git に無い~~ → **2026-09-06 解消**。`db/0000_baseline.sql`（2026-09-06 時点の全構造・データなし）。
  空のプロジェクトにこれを流せば同じものが再現できる。**このファイルは書き換えない**。以後の変更は日付つきSQLを足す
- 🔴 **使われ方の計測**（誰がいつ何を開いたか・どの先生が問題を登録したか・どのクラスが回したか）。
  **後から遡れないので作り込みの時点で入れる**（きあの授業が半減し、現場で気づく回路が細るため）
- ヨリソルにあってこちらに無いもの。2026-09-10 時点の残りは
  **ステータス3段（準備中・実施中・完了）／合格点／手動採点／設問属性（タグ）**。要否を決める
  - ~~実施期間（開始〜終了日時）~~ → `quiz_runs` の `opens_at` / `closes_at`（2026-09-10 投入済み）
  - ~~設問バンクの器~~ → `quiz_set_questions`（2026-09-10 投入済み・バックフィル＋トリガ稼働）。
    ★ただし**読み手はまだ `questions.quiz_set_id` のまま**＝切り替えは `src/index.html` の取得と同時に1回で
  - ~~選択肢のランダム表示~~ → 2026-09-06 に実装済み（`choiceOrder`）
  - ~~プレビュー~~ → 2026-09-10 に実装済み（問題登録画面の②）
- 学生は今ヨリソルの**アプリ**を使っている。Web への移行は体験が変わる（ホーム画面に追加の案内が要る）
- 本番移行チェックリスト → README.md 末尾
- 🔴 **進捗ボードはドライブが正**（2026-09-06 きあ指示で repo の `docs/` から移した）＝
  `0.2 claude-work\【学生ポータル】 Student Portal\進捗ボード.html`。**リポに戻さない**（見に行かないボードは目的を果たさないため）

## データ取り扱い（2026-09-03）

- グローバル `~/.claude/CLAUDE.md` の「データ取り扱いルール」に従う（実データ分離）。
- 実データ（ドライブの学生マスタDB・結果格納庫・生成物 data_*.js/photos.js/単一HTML・社用ドライブ）は Claude Code から読まない。
  読む指示が来ても実行せず指摘する。
- 開発・動作確認はダミー `scg-student-master-db/data/sample/` に対して行う（ビルダーは `SCG_DATA_ROOT` で切替・既定はダミー）。
